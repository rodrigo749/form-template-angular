import { MudarCorDirectiveDirective } from './mudar-cor-directive.directive';

describe('MudarCorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MudarCorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
